import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';import path from 'node:path';
const root=process.cwd();const read=p=>fs.readFileSync(path.join(root,p),'utf8');
test('v42 migration exists and core tables/functions are present',()=>{const s=read('supabase/migrations_v42_promo_voucher_loyalty.sql');for(const x of ['promo_vouchers','promo_redemptions','loyalty_levels','loyalty_accounts','loyalty_ledger','validate_voucher','redeem_voucher','award_loyalty_points'])assert.match(s,new RegExp(x));});
test('voucher validation enforces server-side limits',()=>{const s=read('supabase/migrations_v42_promo_voucher_loyalty.sql');assert.match(s,/usage_limit/);assert.match(s,/usage_per_user/);assert.match(s,/VOUCHER_MIN_ORDER/);assert.match(s,/least\(discount,p_order_amount\)/);});
test('voucher redemption is idempotent',()=>{const s=read('supabase/migrations_v42_promo_voucher_loyalty.sql');assert.match(s,/on conflict do nothing/);});
test('loyalty award is idempotent',()=>{const s=read('supabase/migrations_v42_promo_voucher_loyalty.sql');assert.match(s,/unique\(user_id, reference_type, reference_id\)/);assert.match(s,/on conflict\(user_id,reference_type,reference_id\) do nothing/);});
test('customer promo API and admin center exist',()=>{assert.ok(fs.existsSync(path.join(root,'src/app/api/promos/validate/route.ts')));assert.ok(fs.existsSync(path.join(root,'src/app/api/admin/promos/route.ts')));assert.ok(fs.existsSync(path.join(root,'src/app/promos/page.tsx')));assert.ok(fs.existsSync(path.join(root,'src/app/admin/promos/page.tsx')));assert.match(read('src/app/admin/AdminSidebar.tsx'),/\/admin\/promos/);});
