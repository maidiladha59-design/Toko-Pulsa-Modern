import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';import path from 'node:path';
const root=process.cwd();const read=f=>fs.readFileSync(path.join(root,f),'utf8');
test('v36 platform migration contains requested tables/functions',()=>{const s=read('supabase/migrations_v36_platform_features.sql');for(const x of ['topup_fee_settings','home_banners','testimonials','product_favorites','referrals','missions','kyc_verifications','user_app_settings','bank_transfer_services','category_media','admin_adjust_wallet','process_referral_qualification'])assert.match(s,new RegExp(x));});
test('v36 removes customer search from home navbar and adds QRIS/referral/mission routes',()=>{const home=read('src/app/page.tsx'),nav=read('src/components/Navbar.tsx');assert.doesNotMatch(home,/Cari produk/);assert.doesNotMatch(nav,/Cari produk digital/);for(const x of ['/scan-qris','/referral','/missions','/settings','/notifications'])assert.match(nav,new RegExp(x.replaceAll('/','\\/')));});
test('v36 automatic topup separates credit amount from charge amount',()=>{const api=read('src/app/api/topup/route.ts'),hook=read('src/app/api/payments/fr3newera/webhook/route.ts'),sql=read('supabase/migrations_v36_platform_features.sql');for(const x of ['admin_fee','payment_amount','fee_group','calculateFee'])assert.match(api,new RegExp(x));assert.match(hook,/payment_amount/);assert.match(sql,/topup_fee_methods/);});
test('v36 admin platform and wallet adjustment routes exist',()=>{for(const f of ['src/app/admin/platform/page.tsx','src/app/admin/analytics/page.tsx','src/app/api/admin/platform/route.ts','src/app/api/admin/media/route.ts','src/app/api/admin/users/[id]/wallet/route.ts'])assert.ok(fs.existsSync(path.join(root,f)),f);});
