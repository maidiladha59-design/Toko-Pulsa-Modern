'use client';import {useState} from 'react';import Button from '@/components/Button';
export default function Promos(){const[code,setCode]=useState('');const[amount,setAmount]=useState('');const[result,setResult]=useState<any>(null);const[loading,setLoading]=useState(false);async function check(){setLoading(true);setResult(null);const r=await fetch('/api/promos/validate',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({code,amount:Number(amount)})});const j=await r.json();setResult(r.ok?j:{error:j.message});setLoading(false)}return <div className="mx-auto max-w-2xl space-y-5 p-5"><div><p className="section-kicker">Promo AIDIL STORE</p><h1 className="text-2xl font-black">Voucher & Loyalty</h1><p className="mt-1 text-sm text-slate-500">Cek voucher aktif sebelum checkout dan gunakan promo yang memenuhi syarat.</p></div><div className="rounded-2xl border bg-white p-5 space-y-3"><input value={code} onChange={e=>setCode(e.target.value)} placeholder="Kode voucher" className="w-full rounded-xl border p-3 uppercase"/><input type="number" value={amount} onChange={e=>setAmount(e.target.value)} placeholder="Nominal pesanan" className="w-full rounded-xl border p-3"/><Button onClick={check} loading={loading} className="w-full">Cek Voucher</Button>{result?.valid&&<div className="rounded-xl bg-emerald-50 p-4 text-sm"><b>Voucher valid: {result.code}</b><p>Diskon: Rp{Number(result.discount).toLocaleString('id-ID')}</p></div>}{result?.error&&<div className="rounded-xl bg-red-50 p-4 text-sm text-red-700">{result.error}</div>}</div></div>}
