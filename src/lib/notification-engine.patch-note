V65 adds NotificationEventKey values RANKING_POSITION, RANKING_UP_ONE, and RANKING_CHANGED.
If your current notification-engine.ts does not contain these keys, add them to the union type.
The rest of the engine can remain unchanged.
